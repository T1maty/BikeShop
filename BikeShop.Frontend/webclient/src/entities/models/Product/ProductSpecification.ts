export interface ProductSpecification {
    id: number
    productId: number
    sortOrder: number
    description: string
    name: string

    createdAt: string
    updatedAt: string
    enabled: boolean
}