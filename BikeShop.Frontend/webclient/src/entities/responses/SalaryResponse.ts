export interface SalaryResponse {
    id: number
    userId: string
    rate: number
    shopPercent: number
    workPercent: number
    workshopPercent: number
    createdAt: string
    updatedAt: string
    enabled: boolean
}