export interface CreateShopResponse {
    id: 1
    name: string
    address: string
    phone: string
    secret: string
    storageId: number
    enabled: boolean
    createdAt: string
    updatedAt: string
}